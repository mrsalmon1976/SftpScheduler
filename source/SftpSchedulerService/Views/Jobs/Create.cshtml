@{
    Layout = "_Layout";
    ViewBag.PageTitle = "Jobs - Create New Job";

}
@section PageActionMenu {
}
@section Scripts {
    <script src="~/js/models/host.js"></script>
    <script src="~/js/pages/job/form.js"></script>
}
<div id="app-job">
    <form class="form-horizontal">
        <div class="card card-info">
            <div class="card-header">
                <h3 class="card-title">Job Details</h3>
            </div>
            <div class="card-body">
                <div class="form-group row">
                    <label for="job-name" class="col-sm-2 col-form-label">Name</label>
                    <div class="col-sm-4">
                        <input type="text" class="form-control" id="job-name" placeholder="Name">
                    </div>
                </div>
                <div class="form-group row">
                    <label for="job-host" class="col-sm-2 col-form-label">Host</label>
                    <div class="col-sm-4">
                        <select v-model="host" class="form-control" id="job-host">
                            <option id="{{ h.id }}" v-for="h in allHosts">{{ h.name }}</option>

                        </select>
                    </div>
                </div>
                <div class="form-group row">
                    <label for="job-schedule" class="col-sm-2 col-form-label">Schedule</label>
                    <div class="col-sm-4">
                        <input v-model="schedule" type="text" class="form-control" id="job-schedule" placeholder="? ? ? ? ? ?" autocomplete="false">
                    </div>
                    <div class="col-sm-6">
                        <div v-if="schedule != ''" class="alert inform-alert" :class="{ 'alert-success': isScheduleValid, 'alert-danger': !isScheduleValid }">{{scheduleInWords}}</div>
                    </div>
                </div>
                <div class="card-footer">
                    <button type="button" @@click="submit" class="btn btn-info" :disabled="isLoading">Create Job</button>
                    <button type="reset" class="btn btn-default float-right">Cancel</button>
                </div>

            </div>
        </div>
    </form>
</div>
