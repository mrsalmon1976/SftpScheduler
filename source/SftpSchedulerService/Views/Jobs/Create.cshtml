@{
    Layout = "_Layout";
    ViewBag.PageTitle = "Jobs - Create New Job";

}
@section PageActionMenu {
}
@section Scripts {
    <script src="~/js/models/job.js"></script>
    <script src="~/js/pages/job/form.js"></script>
}
<div id="app-job">
    <form class="form-horizontal">
        <div class="card card-info">
            <div class="card-header">
                <h3 class="card-title">Job Details</h3>
            </div>
            <div class="card-body">
                <div class="form-group row">
                    <label for="job-name" class="col-sm-2 col-form-label">Name</label>
                    <div class="col-sm-4">
                        <input type="text" v-model="job.name" @@blur="job.validateName()" class="form-control" id="job-name" placeholder="Name">
                        <span v-if="!job.isNameValid" class="error invalid-feedback">Please provide a name at least 3 characters long</span>
                    </div>
                </div>
                <div class="form-group row">
                    <label for="job-host" class="col-sm-2 col-form-label">Host</label>
                    <div class="col-sm-4">
                        <select v-model="job.hostId" class="form-control" id="job-host">
                            <option v-for="h in allHosts" :value="h.id">{{ h.name }}</option>

                        </select>
                    </div>
                </div>
                <div class="form-group row">
                    <label for="job-schedule" class="col-sm-2 col-form-label">Schedule</label>
                    <div class="col-sm-4">
                        <input type="text" v-model="schedule" class="form-control" id="job-schedule" placeholder="? ? ? ? ? ?" autocomplete="false">
                        <span v-if="!job.isScheduleValid" class="error invalid-feedback">Please provide a valid schedule</span>
                    </div>
                    <div class="col-sm-6">
                        <div v-if="job.schedule != ''" class="alert inform-alert" :class="{ 'alert-success': job.isScheduleValid, 'alert-danger': !job.isScheduleValid }">{{scheduleInWords}}</div>
                    </div>
                </div>
                <div class="card-footer">
                    <button type="button" @@click="submit" class="btn btn-info" :disabled="isLoading">Create Job</button>
                    <button type="reset" class="btn btn-default float-right">Cancel</button>
                </div>

            </div>
        </div>
    </form>
</div>
